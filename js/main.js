$(function(){
    "use strict"

    // Make Background Size As Same As Window Size
    var winh = $(window).height();
    var navh = $(".navbar").innerHeight();

    $(".header").height(winh);

    
});
